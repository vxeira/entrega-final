version: "3.8"
services:
  web:
    build: ./apps/web
    ports:
      - "8080:80"
    container_name: pwa-web
    depends_on:
      - api
  api:
    build: ./apps/api
    ports:
      - "3000:3000"
    container_name: pwa-api
